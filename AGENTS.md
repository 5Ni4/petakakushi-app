# ぺたかくしの版管理

このアプリは2026-09-08にユーザーが現在の公開状態を **ver1.0** として承認した。

- 基準は製品版 `1.0.0`、Gitタグ `v1.0.0`、コミット `0fd24e513207195f298bf259ca208aa44b2e81aa`。初版を直接書き換えたり、既存タグを付け替えたりしない。
- 以後の改善はバージョン単位で行う。小さな不具合修正はパッチ（例 `1.0.1`）、機能追加や操作改善はマイナー（例 `1.1.0`）。ユーザーが指定した版番号を優先する。
- 次の実装を始めるときに対象版を決め、`work/v<版番号>` ブランチで進める。依頼がない機能を先回りで実装しない。
- 各版で `CHANGELOG.md` に変更点・確認内容・残る制約を記録する。リリースタグはその版の実際の公開ソースに付ける。
- `VERSIONING.md` の保存物と版番号対応を参照する。`../releases/petakakushi-v1.0.0/` の既存保存物を上書きしない。後続版は別フォルダに保存する。
- 現行サイトと保存済みの版を再利用する。版を増やすだけのために別のSitesプロジェクトを作らない。
- 初版の `package.json` の `0.1.0` は雛形由来で、製品版番号ではない。初版の保存ソースは維持し、次の実装版から package.json と package-lock.json の製品版番号を合わせる。
- 画像は端末内で加工する方針と、承認済み14種類の画風を維持する。API課金や画像の外部送信を追加しない。

公開や共有範囲は、その時点のユーザーの依頼と権限に従う。
